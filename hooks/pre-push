#!/bin/bash

# --- Hook pre-push pour bloquer le push si des erreurs détectées ---
# Compatible Windows / Linux / macOS

# Détection automatique du bon exécutable Python
if command -v python3 &>/dev/null; then
    PYTHON_CMD="python3"
elif command -v python &>/dev/null; then
    PYTHON_CMD="python"
else
    echo "❌ Erreur : Python n'est pas installé ou non reconnu dans le PATH."
    echo "Veuillez installer Python et l'ajouter au PATH avant de pousser votre code."
    exit 1
fi

# Détermination du chemin absolu du script Python
# (peu importe où tu exécutes la commande git)
REPO_ROOT=$(git rev-parse --show-toplevel)
SCRIPT_PATH="$REPO_ROOT/scripts/review_mailer.py"

# Vérification que le script existe
if [ ! -f "$SCRIPT_PATH" ]; then
    echo "❌ Erreur : Script introuvable à l'emplacement $SCRIPT_PATH"
    exit 1
fi

# Récupération de l'adresse email de l'auteur du dernier commit
RECIPIENT_EMAIL=$(git log -1 --pretty=format:'%ae')

# Récupération des fichiers modifiés pour ce push
CHANGED_FILES=$(git diff --cached --name-only | paste -sd "," -)

if [ -z "$CHANGED_FILES" ]; then
    echo "Aucun fichier modifié à vérifier."
    exit 0
fi

echo "🔍 Analyse de code avant le push..."
echo "📨 Envoi du rapport à : $RECIPIENT_EMAIL"
echo "📂 Fichiers concernés : $CHANGED_FILES"
echo "---------------------------------------"

# Exécution du script Python d'analyse et d'envoi de mail
"$PYTHON_CMD" "$SCRIPT_PATH" "$RECIPIENT_EMAIL" "$CHANGED_FILES"
EXIT_CODE=$?

# Si des erreurs sont détectées, on bloque le push
if [ $EXIT_CODE -ne 0 ]; then
    echo "🚫 Push annulé : des erreurs de code ont été détectées."
    echo "💌 Un email de rapport a été envoyé à $RECIPIENT_EMAIL"
    exit 1
fi

echo "✅ Analyse terminée : aucun problème détecté. Push autorisé."
exit 0
